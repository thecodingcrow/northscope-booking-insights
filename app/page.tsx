// Placeholder. Real dashboard arrives in the structure phase per the spec
// (see docs/prototype-notes/0001-dashboard-direction.md for the visual direction).

export default function Home() {
  return (
    <main className="flex-1 grid place-items-center bg-stone-50 text-stone-900 px-8 py-24">
      <div className="max-w-md text-center">
        <div className="text-[11px] uppercase tracking-[0.18em] text-stone-400 mb-3">
          Northscope · Booking insights
        </div>
        <h1 className="text-2xl font-semibold tracking-tight">Scaffold ready</h1>
        <p className="mt-3 text-sm text-stone-500">
          The dashboard direction is locked. Build starts after the spec is approved.
        </p>
      </div>
    </main>
  );
}
